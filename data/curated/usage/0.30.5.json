{
  "version": "0.30.5",
  "period": "2026-08-30",
  "items": [
    {
      "title": {
        "zh-TW": "選單列移除動態精靈夥伴，騰出空間顯示 Grok 用量",
        "zh-CN": "菜单栏移除动态精灵伴侣，腾出空间显示 Grok 用量",
        "en": "Menu bar removes animated companions and allocates their space to Grok",
        "ja": "メニューバーの動的コンパニオンを廃止し Grok の表示領域へ移行",
        "ko": "메뉴 막대의 움직이는 동반자 애니메이션을 제거하고 Grok 공간으로 전환"
      },
      "body": {
        "zh-TW": "⟦公仔換燈。⟧ 選單列移除動態精靈同伴，`menubar/critter_frames.py`、共用 NSTimer、`animateCritters_`、`critter_animation_tick()` 與 `refresh.py` 的 `animation_groups`，與 15 個影格及 `assets/critters/lion/` 移除。當 `not hide_grok` 且 `grok_weekly.percent is not None`，由 `assets/grok_mono_menubar.png`、`_grok_menubar_icon()` 在 `_menubar_attributed_title()` 與 `_compose_title()` 兩處把 Grok 加在 Antigravity 後，保留分隔符；`hide_grok` 預設開啟。",
        "zh-CN": "⟦拆除仪表板上的玩偶底座，空位改装警示灯。⟧ 菜单栏移除动态精灵伴侣，`menubar/critter_frames.py`、共用 NSTimer、`animateCritters_`、`critter_animation_tick()` 与 `refresh.py` 的 `animation_groups` 连同 15 帧动画及 `assets/critters/lion/` 文件夹全移除。Grok 接收该位置，在 `not hide_grok` 且 `grok_weekly.percent is not None` 时，通过 `assets/grok_mono_menubar.png` 与 `_grok_menubar_icon()` 在 `_menubar_attributed_title()` 与 `_compose_title()` 两处缀在 Antigravity 后并保留分隔符行为；`hide_grok` 默认维持 `True`。",
        "en": "⟦Like removing a bobblehead figurine and its motorized timer base from a car dashboard to free up a mounting slot for a warning indicator light.⟧ The menu bar no longer displays animated spirit companions, as `menubar/critter_frames.py`, the shared NSTimer, the `animateCritters_` selector, `critter_animation_tick()`, and `refresh.py`'s `animation_groups` data flow have all been removed alongside the 15 animation frames and the `assets/critters/lion/` directory. Grok now takes over that space: when `not hide_grok` and `grok_weekly.percent is not None`, `_menubar_attributed_title()` and `_compose_title()` use `assets/grok_mono_menubar.png` and `_grok_menubar_icon()` to insert Grok right after Antigravity while preserving the existing separator behavior. The `hide_grok` setting remains `True` by default.",
        "ja": "⟦車のダッシュボードから首振り人形とぜんまいタイマーの台座を取り外し、空いた場所を警告インジケーター用のスロットへ改装するようなものだ。⟧ メニューバーでアニメーション表示されていた精霊コンパニオンが廃止され、専用の `menubar/critter_frames.py`、共有 NSTimer、`animateCritters_` セレクタ、`critter_animation_tick()`、および `refresh.py` の `animation_groups` データフローが、15 枚のアニメーションフレームや `assets/critters/lion/` ディレクトリとともに削除された。その空き領域は Grok が引き継ぎ、`not hide_grok` かつ `grok_weekly.percent is not None` の条件下で、`_menubar_attributed_title()` と `_compose_title()` が `assets/grok_mono_menubar.png` と `_grok_menubar_icon()` を用いて Antigravity の後ろに Grok を配置する。既存の区切り文字の動作は維持され、`hide_grok` はデフォルトで `True` のまま維持される。",
        "ko": "⟦차량 대시보드에서 흔들 인형과 태엽 타이머 받침대를 떼어내고, 비워진 자리를 경고 표시등용 소켓으로 개조하는 것과 같다.⟧ 메뉴 막대에서 움직이던 정령 동반자 기능이 빠지면서, 관련 `menubar/critter_frames.py`, 공유 NSTimer, `animateCritters_` 셀렉터, `critter_animation_tick` 함수, `refresh.py`의 `animation_groups` 데이터 흐름이 15개 애니메이션 프레임 및 `assets/critters/lion/` 디렉터리와 함께 모두 삭제되었다. 이제 해당 공간은 Grok이 넘겨받으며, `not hide_grok`이고 `grok_weekly.percent is not None`일 때 `_menubar_attributed_title` 및 `_compose_title` 함수가 `assets/grok_mono_menubar.png`와 `_grok_menubar_icon` 함수를 통해 Antigravity 바로 뒤에 Grok을 배치한다. 기존 구분 기호 동작은 그대로 유지되며, `hide_grok` 설정은 기본값으로 `True`를 유지한다."
      },
      "original": "**The menu bar no longer shows animated spirit companions; Grok now uses their space.** `menubar/critter_frames.py`, the shared NSTimer and `animateCritters_` selector, `critter_animation_tick()`, and `refresh.py`'s `animation_groups` data flow were removed, together with the 15 animation frames and `assets/critters/lion/`. `assets/grok_mono_menubar.png` and `_grok_menubar_icon()` now add Grok after Antigravity in `_menubar_attributed_title()` and `_compose_title()` when `not hide_grok` and `grok_weekly.percent is not None`, preserving the existing separator behavior; `hide_grok` remains `True` by default."
    }
  ]
}
