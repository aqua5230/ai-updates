{
  "version": "0.30.3",
  "period": "2026-08-28",
  "items": [
    {
      "title": {
        "zh-TW": "Token Saver 簡短回覆兼顧好懂，技術術語加註白話並提示下一步",
        "en": "Token Saver keeps short replies plain and clear with term glosses and next steps"
      },
      "body": {
        "zh-TW": "⟦像主管交代交接事項：話要精練且說白話，遇到行話加句白話註釋，結尾交代成果與下一步，並換掉舊指引便條。⟧ Token Saver 讓簡短回覆兼顧好懂。模組 `usage_terse_mode.py` 的 `_DEFAULT_INSTRUCTION` 將白話風格與精簡並列，規定不可避免的技術術語加一次性白話註釋，並要求回覆以結果與下一步收尾；`usage_terse_reminder.py` 的 `_DEFAULT_REMINDER` 也強化此規則。`installer/session_hooks.py` 提高 `TERSE_HOOK_VERSION`，使自我修復函式 `_self_heal_terse_mode()` 主動替換既有 sidecar（隨附設定檔），不再停留在舊措辭。",
        "en": "⟦Like a manager briefing a team handoff: keeping updates brief while speaking plain language, adding a quick plain-words gloss whenever technical jargon is unavoidable, closing with the result and next action, and immediately swapping out obsolete guidelines.⟧ Token Saver now keeps concise replies easy to understand. In `usage_terse_mode.py`, `_DEFAULT_INSTRUCTION` pairs plain language directly alongside brevity, adds a one-time explanation for unavoidable technical terms, and instructs responses to conclude with the concrete result and next step; `usage_terse_reminder.py`'s `_DEFAULT_REMINDER` reinforces this exact requirement. Furthermore, `TERSE_HOOK_VERSION` in `installer/session_hooks.py` was bumped so `_self_heal_terse_mode()` actively replaces existing sidecar config files instead of leaving them on outdated prompt phrasing."
      },
      "original": "**Token Saver now keeps short replies easy to understand.** `usage_terse_mode.py`'s `_DEFAULT_INSTRUCTION` now makes plain language the style alongside brevity, adds a one-time gloss for unavoidable technical terms, and asks replies to close with the result and next step; `usage_terse_reminder.py`'s `_DEFAULT_REMINDER` reinforces the same rule. `TERSE_HOOK_VERSION` in `installer/session_hooks.py` was bumped so `_self_heal_terse_mode()` replaces existing sidecars instead of leaving them on the old wording."
    },
    {
      "title": {
        "zh-TW": "Token Saver 指引自我一致，放行問候表情並依語言載入提示",
        "en": "Token Saver guidance reconciles rules and loads prompt in your language"
      },
      "body": {
        "zh-TW": "Token Saver 提示指引消除矛盾，並自動以在地語言開啟。模組 `usage_terse_mode.py` 的 `_DEFAULT_INSTRUCTION` 允許開頭問候 emoji 與單行工具意圖，但仍排除在回覆正文之外。在 `installer/session_hooks.py` 中，`_write_terse_sidecar()` 將 `detect_lang()` 記錄於 `lang`，兩組 hook（事件發生時觸發的小程式）腳本內的 `_read_sidecar()` 與 `_detect_lang()` 會在環境變數覆蓋後讀取。未設語言變數的工作階段不再預設載入較長英文提示詞；若 sidecar 隨附檔遺失或損毀依然安全備援至英文。⟦宛如多語接待櫃台：服務員可開頭致意並亮出名牌，正文維持精練，依訪客母語遞上簡章而非預設長篇英文，簡章遺失時以英語備援。⟧",
        "en": "Token Saver's prompt instructions now reconcile internal rules and launch directly in your local language. In `usage_terse_mode.py`, `_DEFAULT_INSTRUCTION` now permits an opening greeting emoji and a one-line tool intent while keeping them strictly outside the reply body. Inside `installer/session_hooks.py`, `_write_terse_sidecar()` writes the output of `detect_lang()` under `lang`, and `_read_sidecar()` / `_detect_lang()` in both hook scripts parse it after checking environment overrides. Consequently, sessions lacking explicit language environment variables no longer fall back to the longer English prompt; missing or corrupted sidecar files still safely default to English. ⟦It is like a multilingual reception desk: permitting the greeter to wave and display a one-line badge while keeping the response brief, handing visitors brochures in their native language instead of defaulting to a bulky English manual, and using English only as a safe fallback when records are lost.⟧"
      },
      "original": "**Token Saver's guidance now agrees with itself and starts in your language.** `usage_terse_mode.py`'s `_DEFAULT_INSTRUCTION` now permits the opening-greeting emoji and a one-line tool intent while still excluding them from the reply body. `_write_terse_sidecar()` in `installer/session_hooks.py` records `detect_lang()` under `lang`, and `_read_sidecar()` / `_detect_lang()` in both hook scripts read it after environment overrides, so sessions without language variables no longer default to the longer English prompt; missing or corrupt sidecars still safely fall back to English."
    },
    {
      "title": {
        "zh-TW": "既有提醒掛鉤支援版本比對，過期腳本主動換新不留舊版",
        "en": "Existing reminder hooks upgrade automatically instead of staying stale"
      },
      "body": {
        "zh-TW": "升級 usage 後，既有的 Token Saver 提醒掛鉤（hook，事件發生時觸發的小程式）現在會自動同步最新版。過去 `installer/session_hooks.py` 裡的自我修復函式 `_self_heal_terse_reminder()` 僅在腳本或註冊遺失時重新補上，已安裝的舊版腳本則原樣保留。現在系統透過 `TERSE_REMINDER_HOOK_VERSION` 與 `_installed_terse_reminder_version()` 比對版號並主動替換過期副本，記錄 `update_terse_reminder_hook` 供診斷。⟦恰似大樓巡檢廣播設備：以前檢查員只在設備遺失時才補裝，裝好的舊款從不更換；現在會核對型號主動換新，並在檢修日誌登記換裝明細以供備查。⟧",
        "en": "After upgrading usage, existing Token Saver reminder hooks now update themselves to the latest version automatically. Previously, `_self_heal_terse_reminder()` in `installer/session_hooks.py` only intervened when a script file or its registration was entirely missing, leaving older installed scripts untouched on stale wording. The system now uses `TERSE_REMINDER_HOOK_VERSION` and `_installed_terse_reminder_version()` to compare version numbers and actively replace outdated script copies, logging an `update_terse_reminder_hook` event for future diagnostics. ⟦It is like a building inspector checking public address speakers: previously only replacing a unit if it was missing while leaving obsolete speakers in place, but now verifying serial numbers, actively replacing aged hardware, and logging the maintenance in service records.⟧"
      },
      "original": "**Existing Token Saver reminder hooks now update instead of staying stale.** `_self_heal_terse_reminder()` in `installer/session_hooks.py` previously restored only a missing script or registration, leaving an installed older script unchanged. `TERSE_REMINDER_HOOK_VERSION` and `_installed_terse_reminder_version()` now compare versions and replace stale copies, recording `update_terse_reminder_hook` for diagnosis."
    }
  ]
}
