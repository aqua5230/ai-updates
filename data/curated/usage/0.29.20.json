{
  "version": "0.29.20",
  "period": "2026-08-08",
  "items": [
    {
      "title": {
        "zh-TW": "設定檔的捷徑不再被存檔動作換掉",
        "zh-CN": "保存设置时不再换掉配置文件的快捷方式",
        "en": "Saving settings no longer replaces your config shortcut",
        "ja": "設定保存で設定ファイルへの近道が消えなくなった",
        "ko": "설정을 저장해도 설정 파일 바로가기가 사라지지 않는다"
      },
      "body": {
        "zh-TW": "你若把 `~/.claude/settings.json` 或 `~/.codex/config.toml` 放進 dotfiles（集中管理個人設定檔的儲存庫），這次不用再怕存個設定就和儲存庫走散。這些檔案常做成符號連結（指向另一個位置的捷徑檔）；舊做法先寫暫存檔，再用 `os.replace()` 蓋過目標，原本是防止程式中斷留下半份檔案，卻會把捷徑檔換成普通檔。之後設定雖然存得進去，儲存庫那份卻不再更新。現在會先找到實際的檔案位置，並在同一處建立暫存檔，避免跨檔案系統搬動失敗。像要替住址改收件資料，從前竟把門口的指路牌拆掉；如今信件會送到收件地址。`settings.json`、`config.toml` 和 `hooks.json` 都共用同一套處理。",
        "zh-CN": "如果你把 `~/.claude/settings.json` 或 `~/.codex/config.toml` 放进 dotfiles（集中管理个人配置文件的仓库），现在保存设置不会再让它和仓库走散。这些文件常做成符号链接（指向另一个位置的快捷文件）；旧做法先写临时文件，再用 `os.replace()` 覆盖目标，本意是防止程序中断留下半份文件，却会把快捷文件换成普通文件。之后设置虽然存得进去，仓库那份却不再更新。现在会先找到实际的文件位置，并在同一处创建临时文件，避免跨文件系统移动失败。像要替住址改收件资料，从前竟把门口的指路牌拆掉；如今信件会送到收件地址。`settings.json`、`config.toml` 和 `hooks.json` 都共用同一套处理。",
        "en": "If you keep `~/.claude/settings.json` or `~/.codex/config.toml` in a dotfiles repository, saving a setting will no longer quietly split it from that repository. These files are often symlinks, shortcut files that point elsewhere. The old safe-save routine wrote a temporary file and used `os.replace()` to prevent a crash from leaving half a file, but that replacement swapped out the shortcut itself for a plain file. Settings still saved, while the repository copy stopped changing. It now resolves the real target first and creates the temporary file beside it, avoiding a failed move across filesystems. It was like replacing an address sign instead of delivering mail to the address; now the delivery reaches the real place. `settings.json`, `config.toml`, and `hooks.json` use the same helper.",
        "ja": "`~/.claude/settings.json` や `~/.codex/config.toml` を dotfiles（個人設定ファイルをまとめて管理するリポジトリ）で管理しているなら、設定を保存してもリポジトリとのつながりが黙って切れなくなった。これらは別の場所を指す近道ファイルであるシンボリックリンクにしていることがある。以前の安全な保存処理は、途中で落ちても半端なファイルを残さないため一時ファイルを書いて `os.replace()` で置き換えていたが、近道そのものを通常ファイルに替えてしまっていた。設定は保存されてもリポジトリ側は更新されなかった。今は先に本当の場所を調べ、同じ場所に一時ファイルを作るので、別のファイルシステムをまたぐ移動も避ける。住所への郵便の代わりに案内板を外していたようなもので、今は本当の住所へ届く。`settings.json`、`config.toml`、`hooks.json` は同じ処理を使う。",
        "ko": "`~/.claude/settings.json`이나 `~/.codex/config.toml`을 dotfiles(개인 설정 파일을 모아 관리하는 저장소)로 관리한다면, 설정을 저장해도 저장소와의 연결이 조용히 끊어지지 않는다. 이런 파일은 다른 위치를 가리키는 바로가기 파일인 심볼릭 링크로 만들기도 한다. 예전의 안전 저장 방식은 중간에 프로그램이 멈춰도 반쪽 파일이 남지 않게 임시 파일을 쓴 뒤 `os.replace()`로 바꿨지만, 바로가기 자체를 일반 파일로 바꿔 버렸다. 설정은 저장돼도 저장소 사본은 더는 갱신되지 않았다. 이제는 먼저 실제 위치를 찾고 그 옆에 임시 파일을 만들므로 파일 시스템을 건너는 이동 실패도 피한다. 주소로 우편을 보내야 하는데 현관의 안내판을 떼어 내던 셈이다. 이제 `settings.json`, `config.toml`, `hooks.json`은 같은 처리로 실제 주소에 반영된다."
      },
      "original": "**Saving a setting turned a symlinked config file into a regular one.** Every settings write goes through `_atomic_write_text()`, which writes a temporary file and then `os.replace()`s it over the target — the standard way to make sure a crash mid-write cannot leave half a file behind. `os.replace()` does not follow symlinks, though, so for anyone who manages `~/.claude/settings.json` or `~/.codex/config.toml` from a dotfiles repository, the symlink was silently replaced by a plain file: the repository copy stopped receiving updates, nothing warned, and the divergence only showed up the next time those dotfiles were deployed somewhere else. The target is now resolved to its real path before the replace, and the temporary file is created next to that real path rather than next to the link — putting it beside the link would move the rename across filesystems, which fails outright with `Invalid cross-device link` and would have traded one silent breakage for a louder one. The atomic write itself is unchanged. `statusline_settings.py` had grown its own copy of this sequence and now calls the shared helper, so `settings.json`, `config.toml` and `hooks.json` are all covered by one implementation."
    },
    {
      "title": {
        "zh-TW": "自動修復記錄不再蓋掉剛授權的設定",
        "zh-CN": "自愈记录不再覆盖刚授权的设置",
        "en": "Self-heal logging no longer overwrites fresh permissions",
        "ja": "自動修復ログが新しい許可設定を上書きしなくなった",
        "ko": "자동 복구 기록이 새로 허용한 설정을 덮어쓰지 않는다"
      },
      "body": {
        "zh-TW": "剛允許一項權限，設定卻莫名消失的狀況少了一個來源。自動修復記錄會讀取 `settings.json`、加上一筆紀錄，再把整份檔案寫回去；若 Claude Code 正好在這個空檔寫入新的 `permissions.allow`，舊的記憶體副本便會把它蓋掉。空檔只有幾毫秒，才特別像難以重現的怪問題。現在這段讀取、修改、寫回會拿 `flock`（讓同一檔案一次只給一個人改的鎖）保護。它另用 `usage-settings.lock`，不與每則訊息都會寫入的狀態列鎖共用。像兩位職員同時改同一張表格，先拿走舊副本的人以前會把後來新增的核准欄蓋掉；現在先鎖住表格，輪到誰就由誰寫完。",
        "zh-CN": "刚允许一项权限，设置却莫名消失的情况少了一个来源。自愈记录会读取 `settings.json`、加上一条记录，再把整份文件写回去；如果 Claude Code 正好在这个空档写入新的 `permissions.allow`，旧的内存副本便会把它覆盖。空档只有几毫秒，才特别像难以重现的怪问题。现在这段读取、修改、写回会用 `flock`（让同一文件一次只给一个人修改的锁）保护。它另用 `usage-settings.lock`，不与每条消息都会写入的状态栏锁共用。像两位职员同时改同一张表格，先拿走旧副本的人以前会把后来新增的批准栏盖掉；现在先锁住表格，轮到谁就由谁写完。",
        "en": "A newly allowed permission should no longer disappear for no obvious reason. The self-heal log reads `settings.json`, adds an entry, and writes back the full file. If Claude Code wrote a new `permissions.allow` entry in the tiny gap, the logger's older in-memory copy could overwrite it. The gap lasts milliseconds, which made the bug look mysterious. That read-modify-write sequence now uses `flock`, a lock that lets only one writer change the file at a time. It has its own `usage-settings.lock`, separate from the status-line lock that protects a different file written on every message. Picture two clerks editing one form: the clerk holding an old copy used to erase a newly approved field. Now the form is locked until each clerk finishes their turn.",
        "ja": "許可したばかりの権限が理由もなく消える問題に、一つ原因がなくなった。自動修復ログは `settings.json` を読み、記録を足してファイル全体を書き戻す。ごく短い間に Claude Code が新しい `permissions.allow` を書くと、ログ側がメモリに持つ古い内容でそれを上書きしていた。間隔は数ミリ秒なので、再現しにくい怪現象に見えた。今はこの読み取り、変更、書き戻しを `flock`、一度に一人だけが同じファイルを変えられるようにするロックで守る。毎回のメッセージで別のファイルを書くステータスラインのロックとは分け、`usage-settings.lock` を使う。二人の事務員が同じ用紙を直す際、古い写しを持つ人が新しい許可欄を消していたようなものだが、今は一人ずつ書き終える。",
        "ko": "방금 허용한 권한이 이유 없이 사라지는 문제의 한 원인이 사라졌다. 자동 복구 기록은 `settings.json`을 읽고 기록을 하나 더한 뒤 파일 전체를 다시 쓴다. 아주 짧은 틈에 Claude Code가 새 `permissions.allow` 항목을 쓰면, 기록기가 메모리에 들고 있던 이전 사본이 그것을 덮어쓸 수 있었다. 그 틈은 몇 밀리초뿐이라 재현하기 어려운 이상 현상처럼 보였다. 이제 이 읽기, 수정, 다시 쓰기는 `flock`, 같은 파일을 한 번에 한 사람만 바꾸게 하는 잠금으로 보호된다. 매 메시지마다 다른 파일을 쓰는 상태 표시줄 잠금과는 분리해 `usage-settings.lock`을 쓴다. 두 직원이 같은 서류를 고칠 때 옛 사본을 든 직원이 새 승인 칸을 지우던 것과 같았지만, 이제는 한 사람씩 차례로 끝낸다."
      },
      "original": "**The self-heal log could discard settings Claude Code had just written.** `_append_self_heal_log()` reads `settings.json`, appends an entry and writes the whole file back. Claude Code writes the same file, and anything it stored between that read and that write — a newly granted `permissions.allow` entry, for instance — was overwritten by the older copy still held in memory. The window is milliseconds wide, which is exactly why it would have been diagnosed as a mystery rather than as a race. The read-modify-write is now held under a `flock`, reusing the lock helper the status line already uses. It takes its own `usage-settings.lock` rather than sharing the status line's `usage-status.lock`: that one guards a different file written on every message, and sharing it would serialise two unrelated things for no benefit."
    },
    {
      "title": {
        "zh-TW": "短敏感值不再把整段紀錄塗滿",
        "zh-CN": "短敏感值不再涂满整段记录",
        "en": "Short sensitive values no longer blot out whole transcripts",
        "ja": "短い機密値が記録全体を塗りつぶさなくなった",
        "ko": "짧은 민감값이 기록 전체를 가리지 않는다"
      },
      "body": {
        "zh-TW": "查看使用紀錄時，時間、token（AI 處理與計費的文字單位）數或錯誤碼不會再因一個過短的敏感值變成一片 `[REDACTED]`。程式會從逐字稿隱藏符合敏感名稱的環境變數（程式啟動前預先塞進去的隱藏值）；以前它以子字串替換，若值剛好是 `1`，訊息裡每個 `1` 都被遮掉。現在少於八個字元的值不再列入遮蔽，因為這種敏感值本就不值得保護，硬遮只會毀掉周圍文字。像拿油漆滾筒遮住一個門牌號碼，從前連所有含 `1` 的門牌都一起刷黑；現在只處理足夠長、確實需要保密的內容。",
        "zh-CN": "查看使用记录时，时间、token（AI 处理与计费的文字单位）数或错误码不会再因为一个过短的敏感值变成一片 `[REDACTED]`。程序会从逐字稿隐藏符合敏感名称的环境变量（程序启动前预先塞进去的隐藏值）；以前它按子字符串替换，如果值刚好是 `1`，消息里的每个 `1` 都被遮掉。现在少于八个字符的值不再列入遮蔽，因为这种敏感值本就不值得保护，硬遮只会毁掉周围文字。像拿油漆滚筒遮住一个门牌号码，从前连所有含 `1` 的门牌都一起刷黑；现在只处理足够长、确实需要保密的内容。",
        "en": "When you inspect a transcript, timestamps, token counts, and error codes should no longer turn into `[REDACTED]` because of a very short secret. The program hides sensitive-named environment variables, hidden values supplied before a program starts, by replacing matching substrings. Previously, if a value happened to be `1`, every `1` in the message was hidden. Values shorter than eight characters are now skipped: such a sensitive value is not worth protecting, while masking it destroys surrounding text. It was like using a paint roller to hide one house number and blacking out every address containing `1`; now the roller is reserved for values long enough to need it.",
        "ja": "記録を確認するとき、短すぎる秘密値のせいで時刻、token 数、エラーコードまで `[REDACTED]` だらけになることがなくなった。プログラムは、起動前に渡す隠し値である環境変数のうち、敏感な名前に合うものを記録から隠す。以前は部分一致を置き換えていたため、値がたまたま `1` なら文中のすべての `1` を消していた。今は八文字未満の値を遮蔽しない。この程度の機密値は守る価値がなく、周囲の文章を壊すほうが問題だからである。一つの番地を隠すためにペンキを塗り、`1` を含む全住所まで黒くしていたようなもので、今は十分長い値だけを隠す。",
        "ko": "기록을 볼 때 아주 짧은 비밀값 때문에 시각, token 수, 오류 코드까지 `[REDACTED]`로 가득 차는 일이 없어진다. 프로그램은 시작 전에 넣는 숨은 값인 환경 변수 가운데 민감한 이름에 맞는 값을 기록에서 가린다. 예전에는 부분 문자열을 바꿨으므로 값이 우연히 `1`이면 메시지 안의 모든 `1`이 가려졌다. 이제 여덟 글자보다 짧은 값은 가리지 않는다. 그런 민감값은 보호할 가치가 없고 주변 글자를 망가뜨리기 때문이다. 한 집의 번지를 가리려고 페인트를 칠하다가 `1`이 든 모든 번지까지 검게 칠하던 셈이며, 이제는 충분히 긴 값만 가린다."
      },
      "original": "**A short secret redacted every occurrence of its characters.** `_redact_environment_values()` masks sensitive environment values out of transcripts by substring replacement. Nothing bounded how short a value could be, so an environment variable matching the sensitive-name pattern whose value happened to be `1` turned every `1` in the message into `[REDACTED]` — timestamps, token counts, error codes. Values shorter than eight characters are now skipped; a secret that short is not one worth protecting, and treating it as one destroys the surrounding text."
    },
    {
      "title": {
        "zh-TW": "打包的 instate 工具多了驗貨關卡",
        "zh-CN": "打包的 instate 工具多了验货关卡",
        "en": "Bundled instate tool now gets a verification check",
        "ja": "同梱する instate ツールに検品工程が加わった",
        "ko": "묶어 넣는 instate 도구에 검품 단계가 생겼다"
      },
      "body": {
        "zh-TW": "從原始碼製作 App、電腦又沒有本機 instate 專案時，下載來的 instate CLI（命令列工具，在終端機用文字指令操作的程式）現在先驗過才會被包進 App。過去 `scripts/build_app.sh` 會抓 release 裡的執行檔後直接使用，若下載檔被替換或損壞，沒有檢查能發現。現在會拿 `scripts/instate-cli.sha256` 裡固定的 SHA-256（把檔案算成一串可核對的指紋）比對；不相符便停止建置並刪掉下載檔，不留下可在下次重用的可疑檔案。這份指紋放在此儲存庫，而非和 release 檔放一起，因為能換掉執行檔的人也能一起改旁邊的指紋。下載完全失敗仍只記警告並繼續，少的是面板，不是把不可信的檔案放進去。像收貨時拿店內留存的驗貨單核對封條，而不是相信包裹自己附的紙條。",
        "zh-CN": "从源代码制作 App、电脑又没有本地 instate 项目时，下载来的 instate CLI（命令行工具，在终端里用文字指令操作的程序）现在会先验过才打包进 App。过去 `scripts/build_app.sh` 抓取 release 里的可执行文件后直接使用，如果下载文件被替换或损坏，没有检查能发现。现在会拿 `scripts/instate-cli.sha256` 里固定的 SHA-256（把文件算成一串可核对的指纹）比对；不相符便停止构建并删掉下载文件，不留下可在下次重用的可疑文件。这份指纹放在此仓库，而非和 release 文件放一起，因为能换掉可执行文件的人也能一起改旁边的指纹。下载完全失败仍只记警告并继续，少的是面板，不是把不可信的文件放进去。像收货时拿店内留存的验货单核对封条，而不是相信包裹自己附的纸条。",
        "en": "When building the App from source without a local instate project, the downloaded instate CLI now has to pass inspection before it is bundled. `scripts/build_app.sh` previously downloaded a release binary and used it as-is, so a substituted or corrupted file had no check to catch it. The build now compares it with the fixed SHA-256 in `scripts/instate-cli.sha256`, a fingerprint calculated from a file. A mismatch stops the build and deletes the download, so no suspect binary waits around for the next build. The fingerprint lives in this repository rather than beside the release asset: someone able to replace the binary could replace its neighboring checksum too. A download that fails completely still logs a warning and continues; that means a missing panel, not an untrusted binary. It is like checking a package seal against an inspection sheet kept in the store, not a note inside the package.",
        "ja": "ソースから App を作るとき、ローカルに instate プロジェクトがなければ、ダウンロードした instate CLI（端末で文字の命令を使って操作するツール）は App に入る前に検品される。以前の `scripts/build_app.sh` は release の実行ファイルを取ってそのまま使っており、差し替えや破損を検出できなかった。今は `scripts/instate-cli.sha256` に固定した SHA-256、ファイルから計算する照合用の指紋と比べる。一致しなければビルドを止め、ダウンロードも消すので、次回使われる怪しいファイルを残さない。指紋を release の横でなくこのリポジトリに置くのは、実行ファイルを替えられる人なら横の指紋も替えられるためである。ダウンロード自体が失敗した場合は警告だけで続ける。欠けるのはパネルであり、不審な実行ファイルではない。店に保管した検品票で封を確かめるのと同じである。",
        "ko": "소스에서 App을 만들 때 로컬 instate 프로젝트가 없다면, 내려받은 instate CLI(터미널에서 글자 명령으로 조작하는 도구)는 App에 묶이기 전에 검사를 통과해야 한다. 예전 `scripts/build_app.sh`는 release의 실행 파일을 받아 그대로 썼으므로 바뀌었거나 손상된 파일을 찾아낼 검사가 없었다. 이제 `scripts/instate-cli.sha256`에 고정한 SHA-256, 파일에서 계산하는 대조용 지문과 비교한다. 맞지 않으면 빌드를 멈추고 내려받은 파일도 지워 다음 빌드에 의심스러운 파일이 남지 않는다. 지문을 release 파일 옆이 아니라 이 저장소에 두는 까닭은 실행 파일을 바꿀 수 있는 사람은 옆의 지문도 바꿀 수 있기 때문이다. 다운로드 자체가 실패하면 경고만 기록하고 계속한다. 빠지는 것은 패널일 뿐, 믿을 수 없는 실행 파일을 넣는 일은 없다. 가게에 보관한 검품표로 포장 봉인을 확인하는 것과 같다."
      },
      "original": "**The vendored instate CLI was bundled and executed without being verified.** `scripts/build_app.sh` downloads the binary from a release when the local `instate` project is not present, then packages it into the `.app` as-is. There was nothing to detect a substituted or corrupted download. The build now pins the expected SHA-256 in `scripts/instate-cli.sha256` and refuses to continue when the downloaded binary does not match, deleting it rather than leaving a suspect file behind for the next run to reuse. The fingerprint is committed to this repository rather than published beside the release asset on purpose: a checksum stored next to the binary can be rewritten by whoever rewrites the binary, which verifies nothing. A download that fails outright still logs a warning and continues — that is a missing panel, not a compromised one."
    },
    {
      "title": {
        "zh-TW": "共用快取管線收好，沒用的程式碼也清掉",
        "zh-CN": "收好共用缓存管线，也清掉没用的代码",
        "en": "Shared cache plumbing was gathered up and dead code removed",
        "ja": "共通のキャッシュ処理をまとめ、不要コードも片付けた",
        "ko": "공통 캐시 처리와 죽은 코드를 정리했다"
      },
      "body": {
        "zh-TW": "這次也整理了看不見的後台線路，目標是讓日後修改不必在多處重複動刀。Claude 與 Codex 的分片磁碟快取（把算過／查過的結果暫存起來直接用）原有 63 行相同程式碼，現在把序列化、分片路徑與索引、清除過期快取、讀寫資料搬到共同模組；三個載入器仍各自解析不同格式。它們共通的啟動檢查、延後寫入和結束時盡力寫入，也另收成一個模組；單一 JSON 檔的 agy 快取沒有硬塞進來。另把六個 hook（特定事件發生時被觸發的小程式）判斷與清除函式改成共用邏輯，但保留原名稱作薄包裝。整體刪掉 218 行且行為不變。還移除了死常數、兩個未用函式與 13 個孤兒翻譯鍵；檢查時沒有誤刪動態組出的鍵，也保留外部腳本仍會呼叫的函式。像整理機房線路：共用的線收到同一束，三台不同設備仍接自己的插頭。",
        "zh-CN": "这次也整理了看不见的后台线路，目标是让日后修改不必在多处重复动刀。Claude 与 Codex 的分片磁盘缓存（把算过／查过的结果暂存起来直接用）原有 63 行相同代码，现在把序列化、分片路径与索引、清除过期缓存、读写数据搬到共同模块；三个加载器仍各自解析不同格式。它们共通的启动检查、延后写入和结束时尽力写入，也另收成一个模块；单一 JSON 文件的 agy 缓存没有硬塞进来。另把六个 hook 判断与清除函数改成共用逻辑，但保留原名称作薄包装。整体删掉 218 行且行为不变。还移除了死常量、两个未用函数与 13 个孤儿翻译键；检查时没有误删动态组出的键，也保留外部脚本仍会调用的函数。像整理机房线路：共用的线收到同一束，三台不同设备仍接自己的插头。",
        "en": "Some invisible back-room wiring was tidied so future changes do not require the same edit in several places. Claude and Codex's sharded disk caches, stored results reused directly, had 63 identical lines. Serialization, shard paths and index, stale-cache removal, and payload read/write now live in a common module, while the three loaders still parse their own unrelated formats. Their shared startup check, delayed flush, and best-effort flush at shutdown moved to another module; agy's single-JSON cache was not forced into that shape. Six hook check-and-strip functions now share logic while retaining their original names as thin wrappers. This removes 218 lines with no behavior change. Dead constants, two unused functions, and 13 orphaned translation keys also went away, without deleting runtime-built keys or a function called by an outside script. Like sorting a server room's cables: shared wires go in one bundle, while three different devices keep their own plugs.",
        "ja": "見えない裏方の配線も整理し、今後同じ修正を何か所にも重ねずに済むようにした。Claude と Codex の分割されたディスクキャッシュ（計算や検索済みの結果を一時保存して直接使うもの）には同じ 63 行があり、直列化、分割先のパスと索引、古いキャッシュの削除、データの読み書きを共通モジュールへ移した。三つのローダーは無関係な形式を読むため、解析は別のままである。起動時の確認、間を空けた書き込み、終了時に可能な限り書く処理も別モジュールへまとめた。単一 JSON の agy キャッシュは無理に合わせていない。六つの hook 判定・除去関数も共通化しつつ、元の名前は薄い呼び出し口として残した。動作を変えずに 218 行を減らし、死んだ定数、未使用関数二つ、孤立した翻訳キー 13 個も削除した。動的に作るキーや外部スクリプトが呼ぶ関数は残している。機械室で共通の配線を一束にし、三台の機器は各自の差込口を保つような整理である。",
        "ko": "보이지 않는 뒷단의 배선도 정리해 앞으로 같은 수정을 여러 곳에 되풀이하지 않게 했다. Claude와 Codex의 조각난 디스크 캐시(계산하거나 찾은 결과를 잠시 저장해 바로 쓰는 것)에는 같은 코드 63줄이 있었고, 직렬화, 조각 경로와 색인, 오래된 캐시 제거, 데이터 읽기와 쓰기를 공통 모듈로 옮겼다. 세 로더는 서로 다른 형식을 해석하므로 그 부분은 따로 남겼다. 공통인 시작 확인, 늦춰 쓰기, 종료 시 가능한 만큼 쓰기도 별도 모듈로 모았다. 단일 JSON 파일인 agy 캐시는 억지로 같은 모양에 넣지 않았다. 여섯 hook 판별·제거 함수도 공통 로직을 쓰되 원래 이름은 얇은 호출부로 남겼다. 동작을 바꾸지 않고 218줄을 줄였고, 죽은 상수, 쓰이지 않는 함수 둘, 고아 번역 키 13개도 지웠다. 실행 중 만들어지는 키와 외부 스크립트가 부르는 함수는 남겨 두었다. 기계실에서 공용 선은 한 묶음으로 정리하고 서로 다른 세 장비는 각자 플러그를 유지하는 것과 같다."
      },
      "original": "**Three passages of duplicated code were extracted.** The sharded disk caches for Claude and Codex history held sixty-three identical lines, and `codex_disk_cache.py` reached across to import `history_disk_cache.py`'s private serialisers and re-export them under its own `__all__` — a detour that made the real owner of that code impossible to see. Serialisation, shard paths and index, stale-cache removal and the payload read/write now live in `disk_cache_common.py`. The disk-cache lifecycle shared verbatim by all three loaders — the one-time seed check, the throttled flush and the best-effort flush on termination — moved to `disk_cache_lifecycle.py`. The loaders themselves are still three separate modules and stay that way; they parse three unrelated formats, and only the cache plumbing around them was ever the same. In `session_hooks.py`, three pairs of `_is_X_entry`/`_strip_X_hooks` differing only in a marker constant were parameterised, with the six original names kept as thin wrappers. `agy_disk_cache.py` was deliberately left out: it is a single JSON file with no sharding, and forcing it into the shared shape would have meant changing its behaviour to fit. Net 218 lines removed, with no behavioural change.\n**Dead constants, two unused functions and thirteen orphaned translation keys were removed.** The keys came from renames and from the AI-updates feature moving to its own repository. Finding them took more care than the count suggests: a plain text search over the source reported 159 orphans, of which 146 were false. Keys are frequently assembled at runtime rather than written out — `discussion.html` builds `discussion_status_round1_running` by lowercasing a `SessionStatus` value, and `ui/html_report.py` prepends `report_` to every key it looks up, so four live `report_wrapped_beast_*` entries appear nowhere in the source under their full names. `agy_loader.recent_input_output_tokens()` looks equally dead from inside this repository and was kept: its only caller is a script outside it."
    }
  ]
}
