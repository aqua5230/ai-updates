{
  "version": "2.1.278",
  "period": "2026-09-19",
  "items": [
    {
      "title": {
        "zh-TW": "自動模式改採伺服器端分類器，省下額外判斷開銷",
        "en": "Auto mode switches to server-side classifier, saving extra evaluation overhead"
      },
      "body": {
        "zh-TW": "當你讓 Claude 在自動模式下處理任務時，現在不必再為背後的判斷多付一筆開銷。⟦這好比無人停車場的車牌辨識，進出時由遠端主機驗證放行且免收手續費，只有切換到人工備援窗口時才會提醒加收費用。⟧針對 Claude API、Enterprise 用戶，以及 Bedrock、Vertex、Foundry 與各大閘道，自動模式已改為預設啟用伺服器端分類器。此機制不收取分類器的額外開銷；若退回計費的備援流程，系統會主動發出警告。只要輸入 `/status` 指令，即可在新增的 `Auto mode server` 資料列查看目前工作階段是否在伺服器端運行。若在 Bedrock、Vertex、Foundry 或閘道端想退出，將環境變數設為 `CLAUDE_CODE_AUTO_MODE_SERVER=0` 即可。",
        "en": "When letting Claude handle tasks in auto mode, you no longer need to pay extra overhead for the underlying evaluations. ⟦It is like license plate recognition at an automated parking lot, where entry and exit are verified and cleared by a remote host with no processing fee, and extra charges are alerted only when switching to a manual fallback booth.⟧ For Claude API and Enterprise users, as well as Bedrock, Vertex, Foundry, and major gateways, auto mode now defaults to enabling the server-side classifier. This mechanism does not charge for classifier overhead, and the system actively warns if it falls back to a billed process. Simply run the `/status` command to check whether the current session is running on the server in the newly added `Auto mode server` row. To opt out on Bedrock, Vertex, Foundry, or gateways, simply set the environment variable to `CLAUDE_CODE_AUTO_MODE_SERVER=0`."
      },
      "original": "Changed auto mode for Claude API and Enterprise users, and on Bedrock, Vertex, Foundry and gateways, to default to the server-side classifier, which does not charge for classifier overhead (`CLAUDE_CODE_AUTO_MODE_SERVER=0` opts out on Bedrock, Vertex, Foundry and gateways); warns on billed fallback. See https://code.claude.com/docs/en/auto-mode-classifier-billing\nAdded an `Auto mode server` row to `/status` showing whether this session's auto mode classifier runs on the server"
    }
  ]
}
